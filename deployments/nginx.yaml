apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nginx
  name: nginx
spec:
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      annotations:
        test: testdata
      labels:
        app: nginx
      name: nginx
    spec:
      tolerations:
        - key: testkey
          value: testtestvalue
          effect: NoExecute
          operator: Equal
      nodeSelector:
        name: kk
      containers:
        - name: nginx
          image: "nginx:1.11.1-alpine"
          ports:
          - containerPort: 80
          resources:
            requests:
              cpu: "125m"
              memory: "128M"
